// import gsap from "gsap";

// gsap.registerEffect({
//   name: "spreadChildren",
//   effect: (container: HTMLElement, vars: gsap.TweenVars) => {
//     const children = container?.children;

//     return gsap.fromTo(
//       children,
//       { opacity: 0, scale: 0.95, yPercent: 5, stagger: 0.1, ...vars },
//       { opacity: 1, scale: 1, yPercent: 0 }
//     );
//   },
//   default: {},
// });

export {};
