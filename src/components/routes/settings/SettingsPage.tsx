import styled from "./styled";
import { Typography } from "@mui/material";

const SettingsPage = () => {
  return (
    <styled.Container>
      <Typography variant="h2">SettingsPage</Typography>
    </styled.Container>
  );
};

export default SettingsPage;
