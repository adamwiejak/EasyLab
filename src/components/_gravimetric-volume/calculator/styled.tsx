import styled from "@emotion/styled";
import { Card } from "@mui/material";

const Container = styled(Card)`
  display: grid;
  place-items: center;
`;

const Conponents = { Container };

export default Conponents;
